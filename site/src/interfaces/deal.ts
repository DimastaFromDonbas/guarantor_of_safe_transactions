export interface IDeal {
    id: number,
    createdAt: string,
    updatedAt: string,
    name: string,
    buyer: string,
    seller: string,
    sum: number,
    status: number,
    description: string
}