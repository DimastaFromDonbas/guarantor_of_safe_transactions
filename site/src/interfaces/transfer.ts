export interface ITransfer {
    id: number,
    paymantSystem: string,
    walletNumber: string,
    score: number,
    time: string,
    status: number,
    userEmail: string,
    createdAt: string,
    updatedAt: string,
}